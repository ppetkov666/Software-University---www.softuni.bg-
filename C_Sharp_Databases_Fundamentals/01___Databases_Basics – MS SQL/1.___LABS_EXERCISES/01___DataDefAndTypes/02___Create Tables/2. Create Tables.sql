CREATE TABLE Minions(
	Id BIGINT PRIMARY KEY ,
	[Name] VARCHAR(30) NOT NULL,
	Age INT
)
GO 

SELECT * FROM Minions
GO

CREATE TABLE Towns(
	Id BIGINT PRIMARY KEY,
	[Name] VARCHAR(30) NOT NULL
)
GO

SELECT * FROM Towns